# online-zoo

## The project uses the CSS preprocessor SCSS and normalize.css

### Available Scripts

Compile SCSS to CSS:

```bash
npm run sass
```
Watch SCSS files for changes:

```bash
npm run sass:w
```